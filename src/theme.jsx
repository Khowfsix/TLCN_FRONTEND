import { createTheme } from '@mui/material';

const theme = createTheme({
	palette: {
		primary: {
			main: '#6e5dcf',
			light: 'skyblue',
		},
		secondary: {
			main: '#15c630',
		},
		otherColor: {
			main: '#999',
		},
	},
});

export default theme;
